// iOS has no FirebaseMessagingService — inbound APNs is handled by the AppDelegate (handlers-push.ts).
// This stub lets `import './fcm-service'` resolve on both platforms (NS platform-suffix resolution).
export {};
